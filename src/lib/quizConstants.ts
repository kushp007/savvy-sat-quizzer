/** Number of questions per quiz session. */
export const TOTAL_QUIZ_QUESTIONS = 25;

/** Overall time limit (seconds) — SAT Math module–style cap. */
export const QUIZ_TIME_LIMIT_SEC = 25 * 60;

/** Default batch size when requesting AI questions from the backend. */
export const AI_BATCH_SIZE = 15;
